package com.first.java8.test;

import org.junit.jupiter.api.Test;

import com.first.java8.TimeAndDate;

class TestTimeAndDate {

	@Test
	void testGetDuration() {
		 int decp=(int)Math.pow(10,2);
		TimeAndDate tad = new TimeAndDate();
		//tad.getDuration();
	}

}
